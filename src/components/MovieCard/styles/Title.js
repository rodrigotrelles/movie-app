
import styled from 'styled-components';
import { COLORS } from '../../../assets/resources/colors';

const Card = styled.h2`
    color: ${COLORS.White};
    font-size: 24px;
    line-height: 32px;
    font-weight: 500;
`

export default Card;
